
  # Twitter/X Clone Development

  This is a code bundle for Twitter/X Clone Development

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
